const constants = {
	
	gateway: "http://localhost:8080/gateway/"
	// gateway: "/gateway/"

	// newCohort: "/cohorts/",
	// newAccount: "/accounts/",
	// get: "/retriever/"
}

module.exports = constants